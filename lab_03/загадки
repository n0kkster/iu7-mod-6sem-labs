1. как выбирали базисные функции, они должны быть линейно-независимыми и должны удовлетворять определенным условиям, какие каким условиям, что такое линейно-независимые функции.
2. как сильно меняется погрешность по сравнению с численным методом, когда мы увеличиваем количество базисных функций
3.1. в численном методе меняем краевое условие, слева: u'(0) + u(0) = 1, что изменится в программе в численном методе, объяснить,
3.2 а что если справа стоит не u' = 1, а u' = 1 + u^2?
4. какими методами пытались решить второй вариант с большим коэфф. и решили ли? (не методом прогонки, именно как задачу коши), перечислить методы и аргументировать если не решается
5. сравнить два подхода в 3 задаче - вычисление потока через производную и через интеграл, в том числе в последней точке, где производная односторонняя, как сохраняли порядок точности
6. если 3 задачу будет поставлена так, что на границе будет отражение (к правому краевому условию припишем F = cu + phi(u)), то как будем решать данную задачу
